# Environment Variables

See `.env.example` for the canonical bootstrap list.

- `APP_URL`: public base URL for local development
- `CLOUDFLARE_*`: Cloudflare account and service identifiers
- `AUTH_*`: authentication provider configuration
- `*_PROVIDER`: adapter selection for local-safe or production providers
- `N8N_BASE_URL`: n8n endpoint for workflow orchestration
