# Aimhi Deal Desk architecture

This folder tracks the current architecture, the approved target architecture, and decisions taken while migrating safely.

- `current-state.md` documents what is live today.
- `decisions/` stores architecture decision records.
