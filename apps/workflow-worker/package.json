{
  "name": "@aimhi/workflow-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --config wrangler.jsonc",
    "dev": "wrangler dev --config wrangler.jsonc",
    "lint": "eslint src --ext .ts --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo no-tests"
  },
  "dependencies": {
    "@aimhi/domain": "workspace:*",
    "@aimhi/observability": "workspace:*",
    "wrangler": "^4.114.0"
  }
}
